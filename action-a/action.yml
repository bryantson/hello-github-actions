name: "Hello Actions"
description: "Gree someone"
author: "lovepool@utexas.edu"

inputs:
   MY_NAME:
      description: "Who to greet"
      required: true
      default: "World"
runs:
   using: "docker"
   image: "Dockerfile"
   
branding:
   icon: "music"
   color: red
   
